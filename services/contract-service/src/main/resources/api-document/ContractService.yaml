swagger: "2.0"
info:
  description: REST API documentation for Contract Service
  version: 1.0.0
  title: Contract Service
tags:
  - name: contract
    description: Operations available to working with Contract Service
schemes:
  - "https"
produces:
  - application/json
paths:
  /actuator/health:
    get:
      tags:
        - API Health Check
      summary: Get Health Info
      operationId: getHealthInfo
      responses:
        200:
          description: "Health Info"
definitions:
  Error:
    type: "object"
    properties:
      code:
        type: string
      message:
        type: string
