spring:
  application:
    name: property-service
  datasource:
    url: jdbc:postgresql://localhost:5432/dar360
    username: dar360
    password: dar360_dev
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  flyway:
    enabled: false
server:
  port: 8082
  servlet:
    context-path: /api/properties
services:
  user:
    url: http://localhost:8081
