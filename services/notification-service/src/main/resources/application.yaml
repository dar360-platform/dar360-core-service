spring:
  application:
    name: notification-service
  datasource:
    url: jdbc:postgresql://localhost:5432/dar360
    username: dar360
    password: dar360_dev
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  flyway:
    enabled: false
server:
  port: 8085
  servlet:
    context-path: /api/notifications

# Twilio (placeholder)
twilio:
  account-sid: ${TWILIO_ACCOUNT_SID:your_twilio_account_sid}
  auth-token: ${TWILIO_AUTH_TOKEN:your_twilio_auth_token}
  phone-number: ${TWILIO_PHONE_NUMBER:your_twilio_phone_number}

# SendGrid (placeholder)
sendgrid:
  api-key: ${SENDGRID_API_KEY:your_sendgrid_api_key}
  from-email: noreply@dar360.ae
  from-name: Dar360

# WhatsApp Business API (placeholder)
whatsapp:
  phone-number-id: ${WHATSAPP_PHONE_ID:your_whatsapp_phone_id}
  access-token: ${WHATSAPP_ACCESS_TOKEN:your_whatsapp_access_token}
